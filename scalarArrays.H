#ifndef scalarArrays_H
#define scalarArrays_H

#include "globalHeader.H"

scalarArray initialiseScalarArray(
    const arrayLabel meshDim[2]);

scalarLine initialiseScalarLine(
    const arrayLabel dim);

#endif